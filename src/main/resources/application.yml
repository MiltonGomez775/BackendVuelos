spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flytrack_db
    username: postgres
    password: Soluciondig2003.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

app:
  jwt:
    secret: flytrack-aeropuerto-smart-secret-key-2024-must-be-at-least-32-chars
    expiration: 86400000
